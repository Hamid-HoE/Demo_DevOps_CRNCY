name: CI + CD (Cloud Run)

on:
  pull_request:
    branches: [develop, staging, main]
  push:
    branches: [develop, staging, main]
  # PROD manual: despliegas un digest específico (rollback fácil)
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: "prod"
        type: choice
        options:
          - prod
      image_digest:
        description: "Full image reference by digest (e.g. us-central1-docker.pkg.dev/PROJECT/REPO/crncy-api@sha256:...)"
        required: true
        type: string

concurrency:
  group: cloudrun-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write

env:
  GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GCP_REGION: ${{ secrets.GCP_REGION }}
  GAR_REPO: ${{ secrets.GAR_REPO }}

  WIF_PROVIDER: ${{ secrets.WIF_PROVIDER }}
  WIF_SERVICE_ACCOUNT: ${{ secrets.WIF_SERVICE_ACCOUNT }}

  CLOUD_RUN_RUNTIME_SA: ${{ secrets.CLOUD_RUN_RUNTIME_SA }}

  SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  SONAR_ORG: ${{ secrets.SONAR_ORG }}
  SONAR_PROJECT_KEY: ${{ secrets.SONAR_PROJECT_KEY }}

jobs:
  ci:
    name: CI (tests + SonarCloud)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip

          if [ -f src/requirements.txt ]; then
            pip install -r src/requirements.txt
          elif [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi

          if [ -f src/requirements-dev.txt ]; then
            pip install -r src/requirements-dev.txt
          elif [ -f requirements-dev.txt ]; then
            pip install -r requirements-dev.txt
          fi

          pip install pytest pytest-cov

      - name: Run tests with coverage
        env:
          PYTHONPATH: src
        run: |
          pytest -q --cov=app --cov-report=xml:coverage.xml --cov-report=term-missing

      - name: SonarCloud Scan (blocking on PR, non-blocking on push)
        uses: SonarSource/sonarcloud-github-action@v3
        continue-on-error: ${{ github.event_name == 'push' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ env.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.organization=${{ env.SONAR_ORG }}
            -Dsonar.projectKey=${{ env.SONAR_PROJECT_KEY }}
            -Dsonar.sources=src/app
            -Dsonar.tests=tests
            -Dsonar.python.coverage.reportPaths=coverage.xml
            -Dsonar.coverage.exclusions=tests/**
            -Dsonar.sourceEncoding=UTF-8
            -Dsonar.python.version=3.11

  build_push:
    name: Build & Push Image (Artifact Registry)
    runs-on: ubuntu-latest
    needs: [ci]
    if: ${{ github.event_name == 'push' }}

    outputs:
      image_digest_ref: ${{ steps.build.outputs.image_digest_ref }}
      image_tags: ${{ steps.build.outputs.image_tags }}
      readable_tag: ${{ steps.build.outputs.readable_tag }}
      sha_tag: ${{ steps.build.outputs.sha_tag }}
      digest: ${{ steps.build.outputs.digest }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Auth to Google Cloud (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.WIF_SERVICE_ACCOUNT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for GAR
        run: |
          gcloud auth configure-docker "${GCP_REGION}-docker.pkg.dev" --quiet

      - name: Compute readable tag
        id: meta
        run: |
          set -euo pipefail

          SHORT_SHA="${GITHUB_SHA::7}"
          TS="$(date -u +%Y%m%d-%H%M)"

          if [ "${GITHUB_REF_NAME}" = "develop" ]; then
            PREFIX="dev"
          elif [ "${GITHUB_REF_NAME}" = "staging" ]; then
            PREFIX="test"
          elif [ "${GITHUB_REF_NAME}" = "main" ]; then
            PREFIX="prod"
          else
            PREFIX="ci"
          fi

          TAG_LEGIBLE="${PREFIX}-${TS}-${SHORT_SHA}"

          echo "tag_legible=${TAG_LEGIBLE}" >> "$GITHUB_OUTPUT"
          echo "short_sha=${SHORT_SHA}" >> "$GITHUB_OUTPUT"

      - name: Build, Push (SHA + readable) and export digest
        id: build
        run: |
          set -euo pipefail

          IMAGE_URI="${GCP_REGION}-docker.pkg.dev/${GCP_PROJECT_ID}/${GAR_REPO}/crncy-api"
          SHA_TAG="${GITHUB_SHA}"
          READABLE_TAG="${{ steps.meta.outputs.tag_legible }}"

          echo "Building: ${IMAGE_URI}:${SHA_TAG} and ${IMAGE_URI}:${READABLE_TAG}"

          docker build -t "${IMAGE_URI}:${SHA_TAG}" -t "${IMAGE_URI}:${READABLE_TAG}" .

          # Push SHA tag and capture output to parse digest
          PUSH_OUT="$(docker push "${IMAGE_URI}:${SHA_TAG}")"
          echo "${PUSH_OUT}"

          # Push readable tag (same manifest/digest)
          docker push "${IMAGE_URI}:${READABLE_TAG}"

          DIGEST="$(echo "${PUSH_OUT}" | awk '/digest: sha256:/ {print $3; exit}')"
          if [ -z "${DIGEST}" ]; then
            echo "ERROR: Could not parse digest from docker push output"
            exit 1
          fi

          DIGEST_REF="${IMAGE_URI}@${DIGEST}"

          echo "Immutable digest ref: ${DIGEST_REF}"

          # Export step outputs (consumed as job outputs)
          echo "image_digest_ref=${DIGEST_REF}" >> "$GITHUB_OUTPUT"
          echo "image_tags=${READABLE_TAG},${SHA_TAG}" >> "$GITHUB_OUTPUT"
          echo "readable_tag=${READABLE_TAG}" >> "$GITHUB_OUTPUT"
          echo "sha_tag=${SHA_TAG}" >> "$GITHUB_OUTPUT"
          echo "digest=${DIGEST}" >> "$GITHUB_OUTPUT"

  deploy_dev:
    name: Deploy DEV (develop)
    runs-on: ubuntu-latest
    needs: [build_push]
    if: ${{ github.event_name == 'push' && github.ref_name == 'develop' }}
    environment: DEV

    env:
      CLOUD_RUN_SERVICE: ${{ secrets.CLOUD_RUN_SERVICE_DEV }}
      IMAGE: ${{ needs.build_push.outputs.image_digest_ref }}

    steps:
      - name: Guard (service/image set)
        run: |
          echo "IMAGE from needs.build_push.outputs.image_digest_ref = '${IMAGE}'"
          test -n "${CLOUD_RUN_SERVICE}" || (echo "CLOUD_RUN_SERVICE_DEV is empty" && exit 1)
          test -n "${IMAGE}" || (echo "IMAGE digest is empty (build_push output missing)" && exit 1)
          echo "${IMAGE}" | grep -q "@sha256:" || (echo "IMAGE must be digest ref (...@sha256:...)" && exit 1)

      - name: Auth to Google Cloud (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.WIF_SERVICE_ACCOUNT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy (immutable digest)
        run: |
          gcloud run deploy "${CLOUD_RUN_SERVICE}" \
            --image "${IMAGE}" \
            --region "${GCP_REGION}" \
            --project "${GCP_PROJECT_ID}" \
            --service-account "${CLOUD_RUN_RUNTIME_SA}" \
            --quiet

  deploy_test:
    name: Deploy TEST (staging)
    runs-on: ubuntu-latest
    needs: [build_push]
    if: ${{ github.event_name == 'push' && github.ref_name == 'staging' }}
    environment: TEST

    env:
      CLOUD_RUN_SERVICE: ${{ secrets.CLOUD_RUN_SERVICE_TEST }}
      IMAGE: ${{ needs.build_push.outputs.image_digest_ref }}

    steps:
      - name: Guard (service/image set)
        run: |
          echo "IMAGE from needs.build_push.outputs.image_digest_ref = '${IMAGE}'"
          test -n "${CLOUD_RUN_SERVICE}" || (echo "CLOUD_RUN_SERVICE_TEST is empty" && exit 1)
          test -n "${IMAGE}" || (echo "IMAGE digest is empty (build_push output missing)" && exit 1)
          echo "${IMAGE}" | grep -q "@sha256:" || (echo "IMAGE must be digest ref (...@sha256:...)" && exit 1)

      - name: Auth to Google Cloud (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.WIF_SERVICE_ACCOUNT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy (immutable digest)
        run: |
          gcloud run deploy "${CLOUD_RUN_SERVICE}" \
            --image "${IMAGE}" \
            --region "${GCP_REGION}" \
            --project "${GCP_PROJECT_ID}" \
            --service-account "${CLOUD_RUN_RUNTIME_SA}" \
            --quiet

  deploy_prod_manual:
    name: Deploy PROD (manual, by digest)
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.environment == 'prod' }}
    environment: PROD

    env:
      CLOUD_RUN_SERVICE: ${{ secrets.CLOUD_RUN_SERVICE_PROD }}
      IMAGE: ${{ inputs.image_digest }}

    steps:
      - name: Guard (service/image set)
        run: |
          test -n "${CLOUD_RUN_SERVICE}" || (echo "CLOUD_RUN_SERVICE_PROD is empty" && exit 1)
          echo "${IMAGE}" | grep -q "@sha256:" || (echo "IMAGE must be a digest reference (...@sha256:...)" && exit 1)

      - name: Auth to Google Cloud (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.WIF_SERVICE_ACCOUNT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy PROD (immutable digest)
        run: |
          gcloud run deploy "${CLOUD_RUN_SERVICE}" \
            --image "${IMAGE}" \
            --region "${GCP_REGION}" \
            --project "${GCP_PROJECT_ID}" \
            --service-account "${CLOUD_RUN_RUNTIME_SA}" \
            --quiet